# torch is deliberately NOT pinned here. Kaggle/Colab GPU images ship a pre-installed,
# CUDA-matched PyTorch build; a plain `pip install torch==X` can pull a mismatched or
# CPU-only wheel and silently break GPU acceleration. Use whatever the platform
# provides (verify with `torch.cuda.is_available()`) rather than reinstalling it.
transformers==5.12.1
huggingface_hub==1.20.1
numpy>=1.26,<2.1
pandas==2.2.3
scikit-learn==1.6.1
pytest==9.1.1
